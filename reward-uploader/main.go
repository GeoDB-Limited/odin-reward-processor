package main

func main() {
	//cfg := config.GetConfig(config.NewConfig())
	//
	//rewards, err := RewardsParser()
	//if err != nil {
	//	panic(err)
	//}
	//
	//processor := NewTxProcessor(cfg.Chain, cfg.Signer)
	//for _, reward := range rewards {
	//	msg := banktypes.NewMsgSend(b.Accounts[0].AccountAddress, toAddress, types.NewCoins(types.NewInt64Coin("loki", 100)))
	//}
}
